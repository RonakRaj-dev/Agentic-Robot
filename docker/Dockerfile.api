FROM edubot-base:latest

WORKDIR /app

COPY . .

EXPOSE 8000

ENV PYTHONUNBUFFERED=1
ENV AGENT_MODE=microservice

CMD ["python", "-m", "uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]
